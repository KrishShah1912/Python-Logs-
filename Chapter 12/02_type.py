n : int = 7
# here we have already told that n is integer 
x: str = "Krish"
# here we have already told that x is string

def sum(a: int, b: int) -> int:
    return(a+b)

# here we have already told that a and b both are integers
# and the result will be integer