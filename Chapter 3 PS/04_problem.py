name = "krish is a good  boy yes"
# here we need to detect double space
# if the code doen't have any double space it will print output as -1
print (name.replace("  ", " "))
# here double space will be repalced by single space or normal space
print (name) # here string name is not replaced we have made just change with name.replace
# strings are immutable i.e we can't change