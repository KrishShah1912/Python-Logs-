a = (1,45,67,23,12, "false", "krish", "Rahul")
# tuple are immutable like string that it can't be changed
print (type(a)) # a is tuple

# we can create empty tuple also like a = ()
# a = (1) if you print it's type then it will say int
# instead do this a = (1,) if you print this it will come to know that it's a tuple with only 1 element in it currently

print (a)